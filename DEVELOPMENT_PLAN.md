# Vocabulary Castle League 발전 계획서
## — 데모 시제품에서 "진짜 어휘 학습 게임"으로 —

> 작성: Fable 5 (2026-07-25) · 현재 `index.html` 코드 분석 기반

---

## 1. 현재 상태 정밀 분석 (코드 근거)

### 1.1 실제 구현된 것

`index.html` 단일 파일(약 970줄)에 HTML/CSS/JS가 모두 들어 있으며, 외부 요청·의존성·빌드 도구가 전혀 없는 순수 Vanilla JS 자체 실행형이다. README 명시대로 GitHub Pages 정적 배포와 로컬 더블클릭 실행이 모두 가능하다.

**게임 메커니즘 (관찰 사실):**

- **정보차(Information-Gap) 협동 퍼즐**이 핵심. 4명의 역할(`ROLES` 상수: 정찰자/어휘 수호자/기록자/아이템 관리자)이 한 단어에 대해 서로 다른 단서(뜻·첫 글자와 글자 수·예문·어원 조각)를 나눠 갖고, 한 기기를 `PASS_ORDER` 순서로 돌려 보며(패스앤플레이) 말로 조합해 답을 맞힌다.
- **화면 상태 머신** `SCREENS` 객체 + `go(name)` 라우터로 `entry → briefing → puzzle → gates → locksIntro → puzzle ×2 → gateOpen → throneIntro → throne → victory`의 **완전 선형 시나리오** 1개가 하드코딩되어 있다.
- **"성 공략" 테마의 실체:** 성문 5개(`GATES` 상수) 중 선택 → 자물쇠 2개 해제 → 왕좌의 방 지문 복원. 단, `SCREENS.gates`의 `pick()` 함수가 `if(no === 3)`으로 **3번 문만 항상 정답**이며, 망원경 정찰도 항상 3번(자물쇠 2)·5번(자물쇠 3)만 공개하는 고정 연출이다. 즉 전략 선택처럼 보이지만 실제 분기는 없다.
- **"학교대항"의 실체:** 입장 화면에서 학교명·참가코드·팀명을 입력받아 `game` 객체에 저장하고 순위표에 표기할 뿐, 참가코드 검증·학교 간 실제 대결·집계는 없다. 순위표는 `localStorage`의 `vcl_leaderboard` 키(같은 브라우저 한정, `loadBoard()/saveBoard()`)이므로 "대항전"은 이름뿐이다.
- **역할 순환:** `playerForRole()`이 `game.round`(1→2)에 따라 한 칸 시프트하는 것으로 구현. 라운드가 2개뿐이라 4역할 완전 순환은 이뤄지지 않는다.

**어휘 데이터 (관찰 사실):**

- JS 상수로 **완전 하드코딩**: `PUZZLE_BINOCULARS`, `PUZZLE_OBSTACLE`, `PUZZLE_APPROACH`(단어 3개) + `THRONE`(수능형 지문 1편, 빈칸 6개: obstacle·acquire·interpret·evidence·perspectives·significant). 중복 제외 **총 8개 단어**가 콘텐츠의 전부다.
- 단서 텍스트가 HTML 문자열로 데이터에 직접 박혀 있어(`clues[R_KEEPER]: '뜻(정의): <b>...</b>'`) 콘텐츠와 표현이 분리돼 있지 않다. 난이도·품사·태그·발음 필드 없음.
- **정답 판정:** `val === p.answer` 소문자 완전 일치. `THRONE.blanks`만 `accept` 배열(예: `['perspectives','perspective']`)로 복수 허용형을 지원한다. 오타 허용·유사 철자 피드백 없음.

**상태 저장·학습 추적 (관찰 사실):**

- 세션 상태는 메모리의 `game` 객체(attempts, correct, hintCount, penalty, items 등)뿐이며 새로고침 시 소실된다.
- localStorage에는 **완주 기록(팀·시간·정답률)만** 저장된다. **어떤 단어를 틀렸는지, 누가 무엇을 학습했는지에 대한 데이터는 전혀 없다** — 오답 기록·복습 큐·개인별 이력이 0이다.

### 1.2 "데모 수준"이라고 판단하는 구체적 근거

| # | 결핍 | 코드 근거 |
|---|---|---|
| 1 | 콘텐츠 8단어, 1회 플레이 후 재도전 시 **답을 이미 알고 있음** (재플레이 가치 0) | 퍼즐 3종 + THRONE이 상수로 고정, `btnRetry`는 같은 시나리오 재시작 |
| 2 | 학습 이력·오답 관리·복습이 전혀 없음 | localStorage에 `vcl_leaderboard`만 존재 |
| 3 | 난이도·적응형 요소 없음 | 데이터에 난이도 필드 자체가 없음 |
| 4 | 성문 선택·정찰이 고정 연출(가짜 선택) | `pick()`의 `if(no === 3)` 하드코딩 |
| 5 | 학교대항이 라벨뿐(로컬 순위표) | `LB_KEY = 'vcl_leaderboard'`, README "서버 전송 없음" |
| 6 | 발음·듣기 요소 없음, 문제 유형이 "주관식 철자 입력" 단일 | 오디오/TTS 코드 부재 |
| 7 | 개인 단위 학습 성과 측정 불가(팀 단위 시간·정답률만) | `game.attempts/correct`가 팀 합산 |

강점도 명확하다: 정보차 협동이라는 **교육적으로 근거 있는 코어 메커니즘**, 단서 4종(정의·형태·문맥·어원)이 이미 어휘 지식의 다면성을 반영한 설계, 의존성 제로의 배포 용이성. 발전 계획은 이 코어를 버리지 않고 **콘텐츠 엔진화 + 학습 과학 탑재 + 대항전 실체화**에 집중한다.

---

## 2. 목표 정의

> **"한 번 플레이하는 시나리오 데모"를 → "수천 단어를 반복 가능한 시나리오로 자동 생성하고, 틀린 단어를 다시 만나게 하며, 학교 간 기록이 실제로 집계되는 어휘 학습 게임"으로.**

성공 기준(정량): (a) 어휘 뱅크 1,000단어 이상, (b) 매 판 문제가 달라짐, (c) 개인별 오답 이력이 저장되고 다음 판에 반영됨, (d) 30일 후 재방문 시 복습 큐가 뜸, (e) 참가코드 하나로 여러 팀 기록이 한 순위표에 모임.

---

## 3. 학습 과학(Pedagogy) 설계

### 3.1 현재 게임에 이미 있는 원리 → 강화 방향

| 원리 | 현재 | 강화 |
|---|---|---|
| 능동적 인출(recall) | 철자 직접 입력(재인이 아닌 인출 — 좋음) | 유지하되 유형 다변화(4.3절 유형별 recall/recognition 구분) |
| 정교화(elaboration) | 어원 조각(`ob+sta+cle`) 단서 | 데이터 모델에 `morphemes` 필드 정식화, 모든 단어에 적용 |
| 문맥 학습 | 예문 단서, 수능형 지문 | 단어당 예문 2개 이상, 지문 템플릿화 |
| 생성 효과 + 협동 | 정보차 말하기 전달 | 유지 (게임의 정체성) |

### 3.2 새로 넣을 핵심: 간격 반복(SRS) — Leitner 5-box

SM-2보다 구현·이해가 쉬운 **Leitner 방식**을 채택한다. 협동 게임과의 결합 방식:

- 팀 플레이 중 **첫 시도에 맞춘 단어**는 box+1, **틀린 단어**는 box 1로 리셋. 판정 지점은 이미 존재한다 — `SCREENS.puzzle`의 `submit()`과 `SCREENS.throne`의 `submit()`에서 단어별 성패를 알 수 있으므로, 여기서 `recordResult(wordId, correct)`를 호출하기만 하면 된다.
- box별 재출제 간격: box1=즉시(다음 판), box2=1일, box3=3일, box4=7일, box5=21일.
- **판 구성 알고리즘:** 매 판 단어 슬롯의 40%는 "복습 만기(due) 단어", 60%는 신규 단어. due가 없으면 전부 신규.
- **오답 관리 루프:** 판 종료 화면(현 `victory`)에 "오늘 틀린 단어" 카드를 추가하고, 다음 판 시작 시 첫 자물쇠를 반드시 최근 오답 단어로 출제("복수전" 연출 — 성 테마와 결합: *"지난번 너희를 막았던 자물쇠가 다시 나타났다!"*).

저장 단위는 **개인이 아닌 팀(또는 기기)**부터 시작한다(Phase 1). 개인별 SRS는 계정이 생기는 Phase 3에서.

### 3.3 적응형 난이도

- 단어에 `level`(1~5) 부여. 팀의 최근 10문항 정답률이 85% 이상이면 출제 레벨 +1, 50% 미만이면 -1 (간단한 계단식 — ELO는 과설계).
- 성문 자물쇠 개수를 실제 난이도와 연결: 어려운 문 = 자물쇠 많음 = 고레벨 단어 = 보상(포인트) 큼. 현재 가짜 선택인 `pick()`을 **진짜 리스크-보상 선택**으로 바꾸는 것이 게임 디자인과 학습 과학이 만나는 지점이다.

### 3.4 재인 → 인출 사다리

같은 단어라도 box 단계에 따라 문제 유형을 바꾼다: box1(신규) = 4지선다(재인, 부담 낮음) → box2~3 = 빈칸 첫글자 힌트 인출 → box4~5 = 무힌트 철자 인출·예문 생성. 이렇게 하면 "쉬운 유형"이 낭비가 아니라 학습 단계가 된다.

---

## 4. 콘텐츠·데이터 모델

### 4.1 단어 스키마 (JSON, `data/words.json`)

현재 `PUZZLE_*` 상수의 단서 4종을 그대로 필드로 승격시키면 기존 UI를 거의 수정 없이 데이터 구동형으로 바꿀 수 있다:

```json
{
  "id": "obstacle",
  "headword": "obstacle",
  "pos": "n",
  "ko": ["장애물"],
  "def_ko": "길을 가로막아 나아가지 못하게 하는 것",
  "def_en": "something that blocks your way",
  "examples": [
    { "en": "A huge rock was an ______ on the road to the castle.", "ko": "커다란 바위가 성으로 가는 길의 장애물이었다." }
  ],
  "morphemes": { "parts": ["ob", "sta", "cle"], "gloss": "ob(맞서)+sta(서다) — 길 앞에 맞서 서 있는 것" },
  "first": "o", "length": 8,
  "accept": ["obstacle", "obstacles"],
  "level": 3,
  "tags": ["수능", "중3", "movement"],
  "ipa": "/ˈɒbstəkl/",
  "confusables": ["obstinate", "obscure"]
}
```

- `first`/`length`는 로드 시 계산 가능하지만 명시해 두면 검증에 쓸 수 있다.
- `confusables`는 4지선다 오답 보기 자동 생성용(없으면 같은 level·pos에서 무작위 추출).
- 지문(왕좌의 칼)용 스키마는 현 `THRONE` 구조(`parts`+`blanks`)를 그대로 JSON화하되, `blanks[].wordId`로 단어 뱅크를 참조하게 한다.

### 4.2 규모 확장 전략

- **1차 목표 300단어**(중등 필수), 2차 1,000+(수능 어휘). 태그로 `중1`·`중2`·`중3`·`고1`·`수능` 세트 구분.
- **데이터 소스:** (a) 교육부 기본어휘 목록·수능 기출 빈출어(목록 자체는 공공 성격이나 뜻풀이·예문은 직접 작성 필요), (b) LLM 보조 생성 + **사람 검수**(어원·예문은 오류가 잦으므로 검수 필수), (c) CC 라이선스 사전(Wiktionary 등)은 라이선스 표기 의무 확인. 저장소가 Proprietary 라이선스이므로 CC-BY-SA 데이터를 섞으면 데이터 파일에 한해 라이선스 충돌이 생길 수 있다 — **자체 제작 또는 CC0/CC-BY 소스 권장** (7절 의사결정 포인트).
- **파이프라인:** CSV(스프레드시트 편집) → 변환 스크립트 → `words.json` + 스키마 검증(필수 필드, `accept`에 headword 포함, level 범위). 교사가 CSV만 고치면 되는 구조가 유지보수의 핵심.
- **로딩:** 정적 호스팅에서 `fetch('data/words-jung3.json')`로 세트 단위 지연 로딩. 300단어 JSON은 100KB 미만이라 분할은 1,000단어 이후에 고려. `file://` 직접 실행 호환이 필요하면 `words.js`(전역 변수 할당) 방식 병행.

### 4.3 문제 유형과 데이터의 관계

하나의 단어 레코드에서 **문제 유형을 자동 생성**한다(유형별 콘텐츠를 따로 만들지 않는 것이 확장의 열쇠):

| 유형 | 생성 방법 | 인지 부하 |
|---|---|---|
| 정보차 주관식(현행) | def_ko/first+length/example/morphemes → 역할 4단서 | 인출·상 |
| 4지선다(뜻→단어) | confusables 또는 동레벨 무작위 3개 | 재인·하 |
| 빈칸(예문) | examples의 `______` | 문맥 인출·중 |
| 매칭(단어↔뜻 4쌍) | 동레벨 4단어 묶음 | 재인·하 |
| 듣기 받아쓰기 | Web Speech API `speechSynthesis`로 headword 발음 → 철자 입력 | 인출·상 |
| 어원 조립 | morphemes.parts 섞어 제시 → 순서 배열 | 정교화·중 |

듣기는 **서버 없이** `speechSynthesis.speak()`만으로 구현 가능하다(품질 편차는 있으나 비용 0). `ipa` 필드는 표시용.

---

## 5. 게임 디자인 — 테마를 학습 동기와 연결

### 5.1 구조: 1회 시나리오 → 성 지도(캠페인)

- **성 1채 = 어휘 세트 1개**(예: 중2 세트 = "은빛 성"). 성 내부는 스테이지(성벽→안뜰→탑→왕좌의 방) 4~6개, 스테이지 = 현 `puzzle`/`gates`/`throne` 화면의 재조합. **화면 상태 머신은 이미 있으므로, 하드코딩된 진행(`briefing→puzzle→gates→...`)을 스테이지 배열 데이터로 치환**하는 것이 리팩토링의 전부다.
- 성문 선택을 진짜 선택으로: 문마다 자물쇠 수(문항 수)와 단어 레벨이 다르고, 어려운 문일수록 별점·포인트가 크다. 망원경(전 판 성과로 획득)은 실제로 문 정보를 공개하는 자원이 된다 — 현 `telescopeUsed` 플래그와 페널티(`WRONG_GATE_PENALTY`) 로직을 확장하면 된다.
- **왕좌의 칼(지문 복원)은 성마다 보스전**으로 유지. 지문을 세트당 3~5편 확보해 로테이션.

### 5.2 보상·진행

- **별점(1~3성):** 시간·정답률·힌트 사용으로 산정(재료는 이미 `game.attempts/correct/hintCount/finalTime`에 다 있다). 별로 다음 성 해금.
- **아이템의 기능화:** 현 아이템(`game.items`)은 이모지 수집뿐이다. 망원경=문 정보 공개(기존), 추가로 "방패=오답 1회 페널티 면제", "횃불=첫 글자 공개" 등 **힌트 경제**로 전환. 아이템은 복습 문제(오답 재도전)를 맞춰야 얻게 해 복습 루프에 동기를 건다.
- **오답 루프 연출:** 판 시작 시 "지난 원정에서 놓친 자물쇠"(due 단어) 스테이지를 선봉에 배치(3.2절).

### 5.3 학교대항의 실체화 (단계적)

1. **Phase 1 (서버 없음):** 결과 화면에 "기록 코드"(결과를 압축·서명 없이 base64 인코딩한 문자열) 표시 → 교사가 취합 스프레드시트에 붙여넣기. 저비용으로 "교실 대항전" 운영 가능.
2. **Phase 3 (서버):** 참가코드 = 리그 ID. 같은 코드로 제출된 기록이 서버 순위표에 집계. 시즌제(월간 리셋), 학교 합산 포인트.

---

## 6. 기술 아키텍처

### 6.1 기본 방침: "정적 클라이언트를 최대한 오래 유지"

- **프레임워크 도입 안 함 (Phase 1~2).** 현 코드는 `SCREENS` 라우터·템플릿 리터럴 렌더링으로 이미 구조가 잡혀 있고 970줄 규모다. React 등을 넣으면 빌드 도구·배포 복잡도가 생기는데 얻는 것이 적다. 대신:
  - `index.html` → `index.html + css/style.css + js/`(engine.js: 상태머신·SRS·판정 / screens.js: 화면 / data/*.json)로 **파일 분리**만 한다. GitHub Pages는 그대로 동작.
  - ES 모듈 사용 시 `file://` 실행이 깨지므로, 로컬 실행 요구가 남아 있으면 일반 `<script>` 다중 로드 유지.
- **저장 계층 추상화:** 지금 `loadBoard/saveBoard`가 localStorage를 직접 만진다. `Storage` 인터페이스(get/set: profile, srsState, leaderboard)로 감싸 두면 Phase 3에서 서버 백엔드로 교체할 때 화면 코드를 건드리지 않는다. localStorage 키를 `vcl_v2_*`로 버전링하고 마이그레이션 함수 1개를 둔다.
- **XSS 주의 유지:** 현 코드는 사용자 입력에 `esc()`를 쓰고 있다. 데이터 구동형으로 바꿀 때 단어 데이터의 HTML 포함 여부를 정리해야 한다 — **데이터는 플레인 텍스트로만, 강조는 렌더러가 담당**하는 원칙 권장.

### 6.2 서버 도입 시점과 형태 (Phase 3)

- 트리거: "학교 간 실시간 순위표"와 "개인 계정"이 필요해질 때. 그 전에는 도입하지 않는다.
- 추천: **Supabase**(README 로드맵과도 일치). 테이블 3개면 시작 가능 — `runs`(팀 기록 제출), `leagues`(참가코드), `profiles`(선택). 정적 프론트 + Supabase JS SDK 조합은 GitHub Pages 배포를 유지한 채 붙일 수 있다.
- **부정 방지 한계 인지:** 클라이언트 판정 결과를 제출하는 구조는 조작 가능하다. 학교대항전을 "공식 대회"로 키우려면 서버 측 출제·판정(Edge Function)이 필요 — 이는 Phase 4 과제로 명시하고, Phase 3에서는 "교사 승인 제출"(교사 코드 입력 시에만 리그 반영)로 완화한다.
- 실시간 4인 멀티(각자 기기, Supabase Realtime)는 매력적이지만 **패스앤플레이가 교실 현장(기기 부족)에서 오히려 강점**이므로, 멀티는 대체가 아닌 추가 모드로 Phase 4에 둔다.

---

## 7. 단계별 로드맵

### Phase 0 — 현재 (완료)
정보차 협동 코어, 1회 선형 시나리오, 8단어, 로컬 순위표. **검증된 것:** 메커니즘의 재미와 교육적 컨셉. **미검증:** 반복 학습 가치.

### Phase 1 — 콘텐츠 엔진화 + 오답 루프 (Quick Wins, 약 2~4주 분량)
**목표:** 매 판 다른 문제, 틀린 단어 재출제. 서버·프레임워크 없음.
1. `PUZZLE_*`/`THRONE` 상수를 4.1 스키마의 `words.json`(우선 **100단어** + 지문 3편)으로 추출, 단서 4종을 데이터에서 렌더링. *(가장 임팩트 크고 착수 쉬움 — 기존 화면 코드 재사용률 90%)*
2. 판 구성기: 세트·레벨에서 단어를 뽑아 자물쇠 퍼즐을 동적 생성. `pick()`의 가짜 성문을 실제 난이도 선택으로 교체.
3. `recordResult(wordId, correct)` + localStorage 단어별 이력 + "다음 판 선봉에 오답 단어" 루프.
4. 결과 화면에 "오늘 배운/틀린 단어" 복습 카드(뜻·예문·발음 버튼 — `speechSynthesis`).
**완료 기준:** 같은 팀이 3판 연속 플레이 시 문제 중복이 자연스럽게 관리되고(오답만 재등장), 콘텐츠 추가가 JSON 편집만으로 가능.

### Phase 2 — 학습 시스템 완성 (약 4~6주 분량)
**목표:** SRS·문제 유형 다변화·캠페인 구조.
1. Leitner 5-box(3.2절) + due 40% 규칙, 재방문 시 "복습 원정" 제안.
2. 문제 유형 4종 추가(4지선다·매칭·듣기 받아쓰기·어원 조립)와 box 단계별 유형 사다리.
3. 성 지도: 세트별 성 3채(중1·중2·중3, 각 300단어로 콘텐츠 확장 총 900+), 별점·해금.
4. 아이템 힌트 경제, 역할 4라운드 완전 순환.
5. "기록 코드" 내보내기로 오프라인 학급 대항전 지원.
**완료 기준:** 1주 간격 재방문 시 만기 복습이 정확히 출제되고, 한 성을 3성으로 깨려면 유형 4종 이상을 통과해야 함.

### Phase 3 — 학교대항 실체화 (서버 도입)
**목표:** 참가코드 기반 온라인 리그.
1. Supabase 도입: 기록 제출·리그 순위표·시즌(월간). 저장 계층 교체(6.1절 추상화 덕에 국소 변경).
2. 간단 계정(익명 ID + 닉네임부터; 이메일 로그인은 선택) → 개인별 SRS 이력 동기화.
3. 교사 페이지: 리그 생성, 참가코드 발급, 팀 기록·단어별 정오답 통계 열람(교사 대시보드 최소판).
**완료 기준:** 서로 다른 기기의 두 팀이 같은 참가코드로 한 순위표에 나타나고, 교사가 학급의 최다 오답 단어 Top 10을 볼 수 있음.

### Phase 4 — 확장 (선택적)
실시간 4인 멀티(각자 기기, Realtime 채널), 서버 측 출제·판정(공정성), 전국 시즌 리그, 교사 커스텀 단어 세트 업로드, 영어 의사소통 모드(금지어: 한국어 설명 금지 라운드).

---

## 8. 리스크·의사결정 포인트 (소유자 결정 필요)

| # | 갈림길 | 선택지 | 권고 |
|---|---|---|---|
| 1 | **정적 유지 vs 백엔드** | (a) Phase 2까지 정적 고수 (b) 조기 Supabase 도입 | **(a)**. 학습 가치는 서버 없이 완성되며, 서버는 리그 수요가 실증된 뒤에. 저장 계층 추상화만 미리 해 둘 것 |
| 2 | **어휘 데이터 확보** | (a) 자체 제작(LLM 보조+검수) (b) CC 사전 데이터 활용 | **(a)**. 저장소가 Proprietary이므로 SA(동일조건) 계열 데이터와 충돌. 100단어×필드 8개는 검수 포함 현실적 분량 |
| 3 | **개인 학습 vs 팀 학습 기록** | 팀(기기) 단위 SRS로 시작 vs 처음부터 개인 구분 | 팀 단위로 시작(패스앤플레이 특성상 개인 귀속이 애매). 개인화는 계정 도입(Phase 3)과 함께 |
| 4 | **파일 구조** | 단일 파일 고수 vs 파일 분리 | 분리 권고. 단일 파일은 970줄에서 이미 한계이며, 콘텐츠를 JSON으로 빼는 순간 어차피 다중 파일이 됨. `file://` 실행 지원 여부만 결정 필요 |
| 5 | **대회 공정성** | 클라이언트 판정 신뢰 vs 서버 판정 | Phase 3까지는 "교사 승인 제출"로 충분. 상금·전국 단위가 되면 서버 판정 필수 — 그 전에 홍보하지 말 것 |
| 6 | **핫시트 vs 실시간 멀티** | 패스앤플레이 대체 vs 병행 | 병행. 기기 1대 교실이 실사용 환경의 다수 — 핫시트는 제약이 아니라 차별점 |

**최우선 착수 항목(이번 주에 시작 가능한 것):** Phase 1-1 — `PUZZLE_*` 상수의 JSON 추출과 데이터 구동 렌더링. 이것 하나로 "8단어 데모"와 "확장 가능한 게임"의 경계를 넘는다.
